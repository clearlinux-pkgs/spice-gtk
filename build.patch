--- spice-gtk-0.39/subprojects/spice-common/meson.build~	2020-12-01 16:32:00.000000000 +0000
+++ spice-gtk-0.39/subprojects/spice-common/meson.build	2022-01-21 17:19:52.776309725 +0000
@@ -129,15 +129,6 @@
   py_module = import('python')
   python = py_module.find_installation()
 
-  if get_option('python-checks')
-    foreach module : ['six', 'pyparsing']
-      message('Checking for python module @0@'.format(module))
-      cmd = run_command(python, '-m', module)
-      if cmd.returncode() != 0
-        error('Python module @0@ not found'.format(module))
-      endif
-    endforeach
-  endif
 endif
 
 # smartcard check
